services:
  - type: web
    name: omaha-open
    runtime: node
    plan: free
    buildCommand: "echo no build needed"
    startCommand: node server.js
    envVars:
      - key: ADMIN_PIN
        sync: false
      - key: NODE_ENV
        value: production
      - key: GOOGLE_CLIENT_ID
        sync: false
